# 팀스쿼드 인스타그램 — 게시물 작성 규칙

팀스쿼드 앱(`/mnt/d/Project/team-squad`) 홍보용 인스타그램 게시물을 **주 1회 21개(하루 3개)** 만들어
PR 로 올린다. **PR 병합 = 승인**이고, 병합된 것만 `publish.yml` 이 예약 시각에 발행한다.

## 주간 작업 절차

1. `git checkout main && git pull && git checkout -b week/<YYYY>-W<주차>`
2. 다음 주 월~일 × 슬롯 3개 = `queue/<YYYY-MM-DD>-<1|2|3>.json` 21개와 이미지를 만든다.
3. `scripts/publish.py --check` 위반 0 → 커밋 → push → PR (제목 `week/<YYYY>-W<주차>: 게시물 21개`).
4. 사용자가 PR 에서 검토한다. 고칠 글은 PR 에서 고치고, 뺄 글은 파일을 지운다.

## 슬롯별 유형

| 슬롯 | 발행(KST) | 유형 | 내용 |
|---|---|---|---|
| 1 | 08시 | 기능 소개 | 화면 1장 + 그 기능이 하는 일 한 가지 |
| 2 | 12시 | 운영 고민 | 운영진이 매주 겪는 불편 → 앱이 대신하는 장면 |
| 3 | 19시 | 시작 안내 | 무료·30초 시작·단톡방 병행 등 가입 장벽을 낮추는 글 |

같은 화면을 다시 쓸 때는 다른 유형·다른 대상(감독·총무·팀원)으로 쓴다. 최근 2주 queue 와 캡션 첫 두 줄이 겹치지 않게 한다.

## 캡션 규칙 (`--check` 가 앞의 둘과 한도를 검사한다)

- **첫 줄은 `[앱 홍보]` 로 시작한다.** 해시태그에 `#앱홍보` 를 넣는다. 홍보임을 밝히지 않으면 신고·삭제 대상이다.
- **존댓말.** 짧은 문장, 한 줄에 한 가지.
- 캡션 링크는 눌리지 않는다 → "프로필 링크에서 확인하세요" 로 안내한다.
- 해시태그 10~15개. 고정: `#앱홍보 #팀스쿼드 #조기축구 #풋살 #축구동호회`
- 2,200자 · 해시태그 30개가 인스타 한도다.

## 사실 확인 — 아래 목록에 없는 기능은 쓰지 않는다

출처: team-squad `docs/marketing/promo-copy.md` (2026-08). 새 기능은 team-squad 코드·docs 에서 확인한 뒤 이 목록에 먼저 추가한다.

- 경기 일정을 올리면 전원에게 참석 투표가 자동으로 열린다
- 포메이션을 고르면 참석자가 쿼터별로 자동 배치된다 (출전 시간 고르게 분배)
- 회비 월별 납부 현황판 · 미납자에게 앱이 알림
- 구장 근처에 도착하면 GPS 로 자동 출석 체크
- 득점·도움·출전이 시즌 통계로 쌓인다 — 다득점·도움·공격P·참석·수비·승률·듀오 7부문 순위
- MOM(그날의 최우수 선수) 투표
- 앱을 안 깐 팀원도 미리 등록해 명단·포지션·등번호 관리
- 팀원별 최근 10경기 출석률
- 상대팀별 승·무·패와 최근 5경기 전적
- 감독·코치·총무·팀원 등급별 권한 9종
- 태블릿·PC 브라우저에서도 사용 (웹 버전)
- Google·카카오 로그인 → 팀 이름 → 초대 링크, 30초 시작 · 카카오톡으로 초대
- 계정 하나로 여러 팀 운영
- 광고·결제 없음, 무료

2026-08 이후 추가된 회비 영수증 OCR·쿼터별 통계 등은 team-squad 에서 동작을 확인한 뒤 목록에 넣는다.

## 이미지 규칙

- **JPEG, 1080×1350 (4:5).** 인스타 API 는 JPEG 만 받는다.
- 앱 화면은 **가상 데이터로 띄운 실제 앱 캡처**만 쓴다. 실제 팀·팀원 이름이 보이면 안 된다.
- 경로: `images/<YYYY-MM-DD>-<슬롯>.jpg`
